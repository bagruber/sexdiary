node_modules
dist
.DS_Store
*.log
.vite
tsconfig.tsbuildinfo

# Von `expo prebuild` erzeugt. Nicht committen: die Quelle ist app.json
# plus die Config-Plugins, und ein eingecheckter Stand laeuft davon weg.
android/
ios/

# Bildschirmfotos fremder Apps als Entwurfsreferenz. Sie enthalten
# Klarnamen Dritter und eine Mailadresse — in einem Repo, das nach
# openCode.de soll, waere das nach dem ersten Commit nicht mehr
# ruecknehmbar.
app ui inspiration/
