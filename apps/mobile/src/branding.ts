/**
 * The app's own name, shown in-app. Disguise mode swaps it for a neutral
 * one (see Preferences.disguise). The home-screen icon and OS-level app
 * name can only be changed by a native build, so this is the honest
 * limit of what disguise mode can do today.
 */
export const APP_NAME = "Sexdiary";
